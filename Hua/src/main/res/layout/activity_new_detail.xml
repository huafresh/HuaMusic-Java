<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android">

    <data>
        <variable
            name="newInfo"
            type="com.example.hua.huachuang.bean.news.NewInfo"/>
    </data>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <LinearLayout
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="?attr/huaBagColor"
            android:paddingLeft="10dp"
            android:paddingRight="10dp"
            android:paddingTop="10dp">
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textSize="@dimen/text_size_big2"
                android:textColor="?attr/huaTextColor"
                android:text="@{newInfo.title}"/>
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="10dp"
                android:orientation="horizontal"
                android:visibility="gone">
                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textSize="@dimen/text_size_small2"
                    android:text="@{newInfo.from}"/>
                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textSize="@dimen/text_size_small2"
                    android:layout_marginLeft="5dp"
                    android:text="@{newInfo.pubTime}"/>
            </LinearLayout>
            <TextView
                android:id="@+id/new_text"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@{newInfo.spannable}"
                android:textSize="@dimen/text_size_middle"
                android:lineSpacingMultiplier="1.5"
                android:layout_marginTop="15dp"/>
        </LinearLayout>
    </ScrollView>
</layout>